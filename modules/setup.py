import os, yaml
import modules.gui as gui

# this will be used once i get to making everything user friendly

def setup():
    pass
    #if not os.path.exists(""):
    #    example = open("meow/meow.yml", "w")
    #    example.write("meow meow meow meow meow")
    #    example.close()